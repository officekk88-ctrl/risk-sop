import { redirect } from "next/navigation";
import { AppShell } from "@/components/app-shell";
import { getSession } from "@/lib/auth";
import { getSystemSettings } from "@/lib/mvp-store";
import { updateSettingsAction } from "./actions";
export default async function SystemPage(){const session=await getSession();if(!session)redirect("/login");if(session.role!=="ADMIN")redirect("/dashboard");const settings=await getSystemSettings(session.email,session.role);if(!settings)redirect("/dashboard");return <AppShell email={session.email}><header className="topbar"><div><p className="eyebrow">管理后台</p><h1>流程、规则与系统配置</h1><p className="muted">集中维护组织、提醒、风险门槛、AI 提示词版本和报告模板版本。</p></div></header><form action={updateSettingsAction} className="card form-grid"><div className="form-section-title"><h2>组织与通知</h2><p className="muted">外部短信、邮件和协作平台作为预留通道，站内提醒已启用。</p></div><label className="field field-wide">机构名称<input name="organizationName" defaultValue={settings.organizationName} required /></label><label className="field">提前提醒天数<input name="reminderDays" type="number" min="1" max="90" defaultValue={settings.reminderDays} /></label><label className="field">决策门风险阈值<select name="riskThreshold" defaultValue={settings.riskThreshold}><option value="CRITICAL">重大</option><option value="HIGH">较高</option><option value="MEDIUM">一般</option><option value="INFO">提示</option></select></label><div className="form-section-title"><h2>版本与安全策略</h2><p className="muted">配置变化写入审计日志；历史报告仍保留快照版本。</p></div><label className="field">AI 提示词版本<input name="aiPromptVersion" defaultValue={settings.aiPromptVersion}/></label><label className="field">报告模板版本<input name="reportTemplateVersion" defaultValue={settings.reportTemplateVersion}/></label><label className="field field-wide">允许文件类型（逗号分隔）<input name="allowedFileTypes" defaultValue={settings.allowedFileTypes.join(", ")}/></label><label className="checkbox-field"><input name="decisionGateEnabled" type="checkbox" defaultChecked={settings.decisionGateEnabled}/>启用关键决策门强制审核</label><div className="form-actions"><button className="button" type="submit">保存系统配置</button></div></form><section className="card"><h2>已配置模块</h2><div className="tag-cloud">{["标准流程模板","37项尽调模板","8个决策门","风险评分与阻断","AI结构化输出","报告模板与版本","知识审核发布","操作与AI审计","数据备份恢复","通知通道预留"].map(x=><span className="tag" key={x}>{x}</span>)}</div><p className="muted">上次更新：{settings.updatedBy} · {new Date(settings.updatedAt).toLocaleString("zh-CN")}</p></section></AppShell>}
