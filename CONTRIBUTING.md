# 贡献指南

感谢你参与开馆风控台。我们欢迎缺陷修复、测试、文档、可访问性、国际化和业务流程方面的贡献。

## 开始之前

1. 对较大功能先创建 Issue，说明使用场景、范围、数据影响和验收方式。
2. 安全漏洞不要创建公开 Issue，请遵循 [SECURITY.md](SECURITY.md)。
3. 行为应符合 [CODE_OF_CONDUCT.md](CODE_OF_CONDUCT.md)。

## 本地开发

```bash
cp .env.example .env.local
npm ci
npm run dev
```

不要在配置、测试夹具、日志或截图中提交真实密钥、个人信息、客户材料和生产数据。

## 架构与业务约束

- AI 生成内容必须先作为候选或 `PENDING` 数据保存，经人工审核后才能进入正式风险、报告或已发布知识。
- 只有 `PUBLISHED` 的知识条目可用于正式检索上下文。
- 法律、消防、结构、财税等需要专业资质的结论必须保留人工复核和证据来源。
- 新增数据字段应为旧版 `.data/mvp-data.json` 提供安全默认值。
- 修改状态联合类型时，应同步更新所有 UI 标签、校验 Schema、迁移和测试。
- 服务端变更必须校验项目访问权限，不能只依赖页面隐藏或客户端判断。

## 提交质量

提交 Pull Request 前至少运行：

```bash
npm run test:quality
npm audit --omit=dev --audit-level=high
```

涉及账号、权限、备份或安全边界时，还应运行相应的 smoke test。Pull Request 应说明：

- 解决的问题和用户影响；
- 关键实现与责任边界；
- 数据迁移或兼容性影响；
- 已执行的测试；
- UI 改动的截图或录屏；
- 尚未解决的风险。

请保持提交聚焦，避免混入无关格式化或生成文件。

## Pull Request 评审

维护者会检查功能正确性、权限、数据兼容、审计链、测试和文档。评审通过不代表专业合规认证；涉及专业结论的内容仍需合格专家确认。
