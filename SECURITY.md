# 安全政策

## 支持范围

项目当前处于 MVP 阶段。安全修复仅针对默认分支的最新版本，不承诺为历史版本提供补丁。

## 私下报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交安全报告。不要在公开 Issue、Discussion、Pull Request、日志或截图中披露漏洞细节、密钥、个人信息或客户数据。

报告应尽量包含：

- 受影响的版本、提交或部署方式；
- 可复现步骤和最小化验证样例；
- 实际影响与潜在攻击场景；
- 建议的缓解或修复方式；
- 是否已在任何公开渠道披露。

维护者会尽快确认收到报告、评估影响，并在修复可用后协调披露。请在获得明确同意前避免访问、修改或下载非本人数据，也不要执行会影响服务可用性的测试。

## 部署方责任

部署方必须自行：

- 替换所有示例密钥和管理员凭据；
- 使用 HTTPS、安全 Cookie 与可信反向代理配置；
- 限制数据库、上传文件、备份和日志的访问；
- 配置依赖更新、备份恢复和安全监控；
- 对权限、隐私、数据保留和适用法规进行独立评估；
- 确保 AI 结果经过人工审核，专业事项由合格人员确认。

仓库中的自动化测试和历史测试报告不构成生产环境安全保证。
